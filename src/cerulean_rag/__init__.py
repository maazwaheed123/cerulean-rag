"""Retrieval-augmented assistant over the 13-document Cerulean Systems corpus.

Generation and embeddings run locally through Ollama. The entry point is
pipeline.ask(question); cli exposes it as `ask` and `chat`.
"""

__version__ = "0.1.0"
